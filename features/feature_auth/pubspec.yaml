name: feature_auth
description: Microapp de autenticación (login de personal).
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^14.0.0
  core_auth:
    path: ../../core/core_auth
  core_ui:
    path: ../../core/core_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
