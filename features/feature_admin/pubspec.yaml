name: feature_admin
description: Panel de administración del tenant.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^14.0.0
  cached_network_image: ^3.4.1
  core_auth:
    path: ../../core/core_auth
  core_network:
    path: ../../core/core_network
  core_ui:
    path: ../../core/core_ui
  core_utils:
    path: ../../core/core_utils
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
