name: core_auth
description: Autenticación, sesión y providers Riverpod del tenant.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^14.0.0
  supabase_flutter: ^2.12.0
  core_network:
    path: ../core_network

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
